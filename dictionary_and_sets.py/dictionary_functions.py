#dictionary functions

mydict={}

mydict.keys() #returns all keys

mydict.values() #returns all values

mydict.items() #returns all (key,value) pairs as tupples

""" pairs[0] will show the 1st (key,value) pair"""

mydict.get()
