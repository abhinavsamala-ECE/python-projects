string = "the Dragonfruit is superior to the apple."
print(string.endswith("uit.")) #prints true if string end with "uit." else false

print(string.startswith("the")) #prints true if string starts with "The" else false

print(string.capitalize()) #capitalizes the first letter of the string

"""
similarly we have:
1. string.replace(old,new) - replaces the old substring with the new substring (works for both letters and words)
2.string.find(substring) - returns the first index of the first substring in the string. else returns -1
3.string.count(substring) - returns the number of times the substring occurs in the string



"""

